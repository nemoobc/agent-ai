---
description: "Audit full: correctness + security + termux-fit. Read-only"
agent: autodev
---
Audit semua file project: logic, edge case, injection (SQL/shell/eval), path traversal, hardcoded secret, port binding, termux-fit (root/proot dep, path luar $HOME). Output: daftar temuan per severity (high/med/low) + file:baris + saran fix. JANGAN ubah file. Lapor dulu, tunggu perintah.
